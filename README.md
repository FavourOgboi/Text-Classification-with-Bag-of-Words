# Text Classification with Bag of Words

A binary classifier that detects **insincere questions** on Quora — questions that are trolling, rhetorical, or asked in bad faith rather than seeking a genuine answer — using classical NLP (tokenization, stopword removal, stemming, Bag-of-Words) and Logistic Regression.

Dataset: [Quora Insincere Questions Classification](https://www.kaggle.com/c/quora-insincere-questions-classification) (Kaggle competition, ~1.3M labeled questions).

## Why this project is more than "ran a tutorial"

The dataset is **~94% sincere / ~6% insincere**. A model that predicts "sincere" for every question scores ~94% accuracy while learning nothing. That makes accuracy a misleading headline metric here — the notebook explicitly compares the trained model against a majority-class baseline and a random baseline, and reports F1 as well as accuracy, so the "did this model actually learn anything" question has an honest answer instead of an inflated one.

The ~1-point accuracy gain over the baseline is misleading on its own — the F1 gain (0.41 vs. 0.00) is what shows the model correctly identifies a meaningful share of insincere questions the naive baselines miss entirely.

## Pipeline

1. **Tokenization** — split questions into words/punctuation with NLTK's `word_tokenize`
2. **Stopword removal** — drop common low-signal words ("the", "is", "a"...)
3. **Stemming** — collapse word forms (`going` → `go`) with `SnowballStemmer`
4. **Vectorization** — `CountVectorizer` (Bag-of-Words), vocabulary capped at 1000 features
5. **Model** — `LogisticRegression` (scikit-learn), trained on a 70/30 train/validation split

## Repo contents

- `text-classification-bag-of-words.ipynb` — the full notebook: EDA, preprocessing, modeling, evaluation, and a written limitations/next-steps section
- `requirements.txt` — Python dependencies
- `submission.csv` — generated by the notebook (not tracked in git; see `.gitignore`)

## Running it

1. Get a Kaggle API key (`kaggle.json`) — [instructions](https://www.kaggle.com/docs/api#authentication)
2. `pip install -r requirements.txt`
3. Open `text-classification-bag-of-words.ipynb` and run all cells. The first code cell downloads the dataset via the Kaggle CLI (or reads from `../input/...` automatically if run inside a Kaggle kernel).

## Limitations & what I'd try next

- **Bag-of-Words ignores word order** — "not good" and "good not" vectorize identically. N-grams or a sequence model would help.
- **`max_features=1000` was a speed-driven default, not tuned** — worth sweeping vocabulary size, or switching to TF-IDF.
- **No class-weighting or resampling** for the 94/6 imbalance (`class_weight='balanced'` is an easy next step).
- **Stemming, not lemmatization** — faster but produces non-words (`supposedly` → `suppos`).
- **No hyperparameter search** on the Logistic Regression itself, and no error analysis on the false negatives.
